; Loaded via PHPRC by the integration-test runs (see README). Child
; PHP processes spawned by the test framework (its own setup:install)
; inherit PHPRC, so this reaches them too — unlike -d flags on the
; phpunit process, which the 128M default would starve mid-install.
memory_limit = -1
